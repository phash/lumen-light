# Kopiere zu .env und passe Werte an

POSTGRES_USER=lumen
POSTGRES_PASSWORD=bitte-aendern-strong-password
POSTGRES_DB=lumen

# Generieren mit: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=bitte-zwingend-mit-zufaelligem-secret-ersetzen

CORS_ORIGIN=https://lumen.example.com
